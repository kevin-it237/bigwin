class Package {
  int id;
  String name;
  int price;
  String interval;
  int intervalCount;
  String categoryTip;
  Package({this.id, this.name, this.price, this.interval, this.intervalCount, this.categoryTip});
}