class Package {
  int id;
  String name;
  double price;
  String interval;
  int intervalCount;
  String categoryTip;
  Package({this.id, this.name, this.price, this.interval, this.intervalCount, this.categoryTip});
}